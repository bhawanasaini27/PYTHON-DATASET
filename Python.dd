**Steps Performed**
1. Renamed Columns 
   - Converted all column names to lowercase and replaced spaces with underscores.
2. Handled Missing Values 
   - Filled missing values in `rating` and `duration` with `'Unknown'`
   - Retained missing values in `director`, `cast`, `country`, and `date_added` as `NaN` for future exploration.
3. Removed Duplicates 
   - Dropped any duplicate rows to ensure clean records.
4. Fixed Data Types
   - Converted `date_added` to proper datetime format.
5. Cleaned Output  
   - Final dataset is clean and analysis-ready.
